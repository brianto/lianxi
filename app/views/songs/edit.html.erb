<% content_for :head do %>
  <script>
    window.globals.urls.get = "<%= song_path(@song, :format => :json) %>";
  </script>
<% end %>

<%= render :partial => "form", :locals => {
  :action => "update",
  :method => "put"
} %>
