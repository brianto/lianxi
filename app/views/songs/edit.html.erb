<% content_for :head do %>
  <script>
    window.globals.urls.get = "<%= song_path(@song, :format => :json) %>";
  </script>
<% end %>

<% content_for :navigation do %>
  <%= render_actions @song, :delete %>
<% end %>

<%= render :partial => "form", :locals => {
  :action => "update",
  :method => "put"
} %>
