<%= form_for [@teachable, @card, @example],
  :url => { :action => "create" },
  :html => { :class => "form-horizontal" },
  :method => :post do |form| %>

  <fieldset>
    <legend>Characters</legend>

    <%= render_field form, @errors, :simplified, :text_area, {
      :rows => 2,
      :placeholder => "ex. 汉字"
    } %>

    <%= render_field form, @errors, :traditional, :text_area, {
      :rows => 2,
      :placeholder => "ex. 漢字"
    } %>
  </fieldset>

  <fieldset>
    <legend>Pronunciation</legend>

    <%= render_field form, @errors, :pinyin, :text_area, {
      :rows => 2,
      :placeholder => "ex. hàn zì"
    } %>

    <%= render_field form, @errors, :zhuyin, :text_area, {
      :rows => 2,
      :placeholder => "ex. ㄏㄢˋ ㄗˋ"
    } %>

    <%= render_field form, @errors, :jyutping, :text_area, {
      :rows => 2,
      :placeholder => "ex. hon6 zi3"
    } %>
  </fieldset>

  <fieldset>
    <legend>Language</legend>

    <%= render_field form, @errors, :translation, :text_area, {
      :rows => 2,
      :placeholder => "ex. noun"
    } %>

    <%= render_field form, @errors, :notes, :text_area, {
      :rows => 2,
      :placeholder => "ex. chinese characters"
    } %>
  </fieldset>

  <%= form.submit :class => "btn btn-primary" %>
<% end %>
