<div class="navbar navbar-default" role="navigation" ng-controller="NavigationController">
  <div class="navbar-header">
    <a class="btn btn-navbar" data-toggle="collapse" data-target="#navbar-content">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <%= link_to "練習", root_path, :class => "navbar-brand" %>
    </a>
  </div>

  <div id="navbar-content" class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-left">
      <button id="pref-charset"
        ng-click="handlers.charset()"
        class="btn btn-link input-small">
        {{ display.charset() }}
      </button>

      <button id="pref-transcript"
        ng-click="handlers.transcript()"
        class="btn btn-link input-small">
        {{ display.transcript() }}
      </button>
    </ul>

      <% if @user.nil? or not @user.valid? %>
        <%= render :partial => "partials/sign_in" %>
      <% else %>
        <%= render :partial => "partials/sign_out" %>
      <% end %>
  </div>
</div>
