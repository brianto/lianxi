<div class="navbar navbar-default" role="navigation" ng-controller="NavigationController">
  <div class="navbar-header">
    <a class="btn btn-navbar" data-toggle="collapse" data-target="#navbar-content">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <%= link_to "練習", root_path, :class => "navbar-brand" %>
    </a>
  </div>

  <div id="navbar-content" class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-left">
      <li>
        <a id="pref-charset"
          ng-click="handlers.charset()"
          class="btn btn-link">
          {{ display.charset() }}
        </a>
      </li>

      <li>
        <a id="pref-transcript"
          ng-click="handlers.transcript()"
          class="btn btn-link">
          {{ display.transcript() }}
        </a>
      </li>

      <% unless @user.nil? %>
        <li class="dropdown">
          <a class="dropdown-toggle btn btn-link" data-toggle="dropdown">
            Create <b class="caret"></b>
          </a>

          <ul class="dropdown-menu">
            <li><%= link_to "Drill", new_drill_path %></li>
            <li><%= link_to "Passage", new_passage_path %></li>
            <li><%= link_to "Song", new_song_path %></li>
          </ul>
        </li>
      <% end %>
    </ul>

    <% if @user.nil? %>
      <%= render :partial => "partials/sign_in" %>
    <% else %>
      <%= render :partial => "partials/sign_out" %>
    <% end %>
  </div>
</div>
