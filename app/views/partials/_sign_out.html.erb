<%= form_tag logout_path, :class => "navbar-form pull-right" do %>
  <div class="form-group">
    <%= link_to @user[:username], @user, :class => "btn btn-link" %>
  </div>

  <%= submit_tag "Sign out", :class => "btn btn-default" %>
<% end %>
