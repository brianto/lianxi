<% content_for :head do %>
  <script>
    window.globals.urls.get = "<%= drill_path @drill, :format => :json %>";
  </script>
<% end %>

<% content_for :navigation do %>
  <%= render_actions @drill, :delete %>
<% end %>

<%= render :partial => "form", :locals => {
  :action => "update",
  :method => "put"
} %>
